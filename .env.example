# Microsoft Clarity session replay for the wisecall.io marketing site.
# Value is the project ID from https://clarity.microsoft.com (Settings → Overview).
# Leave empty (or omit) so Clarity does not load.
NEXT_PUBLIC_CLARITY_PROJECT_ID=
